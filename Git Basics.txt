Initialize and snapshot work.

git init
printf "Hello\n" > README.md
git add README.md
git commit -m "Add README"

After creating one commit, explore status and history.
git status
git log --oneline --graph --decorate --all

Stage precisely with patch mode, then commit meaningful messages.
echo "tip" >> README.md
git add -p README.md
git commit -m "Add quick tip to README"

Create a remote, fetch, and push safely.

git remote add origin https://github.com/user/repo.git
git fetch origin
git branch --set-upstream-to=origin/main main
git push -u origin main

Recover from mistakes without losing work by using the stash and restore.
echo "WIP" >> app.js
git stash push -m "wip app"
git pull --rebase
git stash pop

Between snippets remember: the index is authoritative for the next commit. Stage only what you intend to record.



what does of this commands do? echo "WIP" >> app.js git stash push -m "wip app" git pull --rebase git stash pop

echo "WIP" >> app.js

Adds the text "WIP" to the end of app.js
This simulates you making changes to a file (work in progress)
git stash push -m "wip app"

Stashes your uncommitted changes (both staged and unstaged)
The -m "wip app" adds a message so you can identify this stash later
Your working tree becomes clean — changes are safely stored away
Use case: You're mid-feature but need to switch branches urgently (e.g., fix a bug)

git pull --rebase

Fetches the latest changes from the remote
Rebases your local commits on top of the remote branch (instead of merging)
This keeps history linear and cleaner than a merge commit
Why rebase here? You stashed your work, so there's nothing to conflict with
git stash pop

Retrieves your stashed changes and applies them back to your working tree
Removes the stash after applying (unlike git stash apply, which keeps it)
Your WIP is restored, ready to continue


Add a missing file and amend the last commit without changing its message.

bash
echo "tests" > .gitkeep
git add .gitkeep
git commit --amend --no-edit
Revise the message directly.

bash
git commit --amend -m "Refactor parser: extract tokenizer"
Force-push only when appropriate and with coordination.

bash
git push --force-with-lease
Amend staged hunks precisely using patch mode before amending.

bash
git add -p src/parser.js
git commit --amend --no-edit

Recover the pre-amend state if necessary by keeping the reflog handy.

bash
git reflog
# find the previous HEAD, then
git reset --hard <old_sha>

How to complete this safelyRun the reflog command in your terminal to see a chronological record of every time your HEAD pointer has moved:bashgit reflog
Use code with caution.Identify the correct state from the output. The log will look similar to this:textab12cd3 HEAD@{0}: reset: moving to HEAD~1
ef56gh7 HEAD@{1}: commit: Added the new login feature
ij89kl0 HEAD@{2}: checkout: moving from main to dev

Use code with caution.Execute the hard reset to the target state. You can use either the specific commit SHA or the simpler HEAD@{n} shortcut identifier displayed in the log:bash# Option A: Using the SHA
git reset --hard ef56gh7

# Option B: Using the HEAD relative shortcut (often safer and easier)
git reset --hard HEAD@{1}